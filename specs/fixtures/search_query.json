{
  "$comment": "Search query issued by principal-user-123",
  "querySpec": {
    "queryText": "Dashboard",
    "filters": {
      "status": "published"
    },
    "facets": null,
    "pagination": {
      "offset": 0,
      "limit": 10
    }
  },
  "executionContext": {
    "tenantId": "tenant-001",
    "principalId": "principal-user-123",
    "correlationId": "corr-search-001"
  },
  "$semantics": {
    "tenant_scoping": "Query is implicitly scoped to tenantId from context",
    "permission_filtering": "Results MUST be filtered by permissionAttributes before return",
    "text_search": "queryText performs case-insensitive substring match across indexed fields",
    "filter_application": "filters are exact-match constraints applied after text search"
  }
}
