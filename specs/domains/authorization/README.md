# Authorization

**Platform:** Spine
**Status:** Active — content migrated from legacy locations.

## Purpose

RBAC/ABAC, policy bundles, permissions, decisions — what a caller is allowed to do.

## Capabilities

TBD. List capabilities here as they're scoped. Capabilities are the agent
ownership unit — one capability ≈ one agent.

## Cross-references

- Spec: [./authz.md](./authz.md) — ABAC, deny-overrides-allow, permission/role integration
- Spec: [./security.md](./security.md) — roles, permissions patterns
- Spec: [./authz-module/](./authz-module/) — policy module manifest
- Adapter: `adapters/policy-cedar/` (Cedar engine), `adapters/policy-stub/`
