# Catalog

**Platform:** Content
**Status:** Active — content migrated from legacy locations.

## Purpose

Structured entities, taxonomy, attributes, filters, variants. The structured-content backbone — families, variants, attribute schemas, taxonomy navigation, search.

## Capabilities

TBD. List capabilities here as they're scoped. Capabilities are the agent
ownership unit — one capability ≈ one agent.

## Cross-references

- Module: `modules/catalog/` (handlers, projections, queries, seed types)
- Behavioral spec: `specs/domains/catalog/features/` — Gherkin features run by Playwright BDD; step definitions live in `tests/bdd/steps/catalog/`
- Spec: [./structured-catalog/](./structured-catalog/) — governance manifest
