# Delivery

**Platform:** Content
**Status:** Stub — domain shape committed, content TBD.

## Purpose

Rendered runtime pages, public/private delivery, cacheable read models. The serving side of content — read-dominant, high-concurrency, edge-cacheable.

## Capabilities

TBD. List capabilities here as they're scoped. Capabilities are the agent
ownership unit — one capability ≈ one agent.

## Cross-references

- Module: `modules/content-pages/` (render-tree projection — the artifact Delivery serves)
- Port: `ports/src/render-tree-store.ts`
