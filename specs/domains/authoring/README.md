# Authoring

**Platform:** Content
**Status:** Active — content migrated from legacy locations.

## Purpose

Page editor, layout editor, block editor, publishing workflows. The creation side of content — separated from Delivery so each can scale independently.

## Capabilities

TBD. List capabilities here as they're scoped. Capabilities are the agent
ownership unit — one capability ≈ one agent.

Planned: layout-dsl (TypeScript DSL that compiles to JSON for the platform to consume).

## Cross-references

- App: `apps/authoring/` (page-editor, block-editor, layout-editor, page-gallery)
- Module: `modules/content-pages/` (page CRUD + render-tree projection)
- Spec: [./content-pages/](./content-pages/) — page CMS governance manifest
- Spec: [./content-pages.json](./content-pages.json) — schema/fixture
- Spec: [./page-templates.md](./page-templates.md) — template manifest, regions, page document, editor, 10 invariants
