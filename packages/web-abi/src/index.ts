// @atlas/web-abi — Ring web-abi (frontend stack). Pure wire-contract types
// shared by @atlas/web-kernel (browser) and apps/web-bff (server). Zero deps.
// Per ADR 0017. Wire types land in PR3; this is the scaffold.
export {};
