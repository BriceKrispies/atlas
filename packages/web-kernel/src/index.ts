// @atlas/web-kernel — Ring web-kernel (frontend stack, innermost). The
// in-browser kernel: render/surface/element registry + the reactive
// query/mutate/channel primitives (constitution C14/C15), talking to web-bff.
// Per ADR 0017 + specs/frontend/web-kernel.md. Implementation lands in PR4.
export {};
