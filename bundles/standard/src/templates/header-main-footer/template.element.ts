/**
 * HeaderMainFooterTemplate — full-width header, main region, full-width
 * footer. CSS-only chrome; see ../templates.css for the grid rules.
 */

import { AtlasElement } from '@atlas/core';

export class HeaderMainFooterTemplate extends AtlasElement {}

AtlasElement.define('template-header-main-footer', HeaderMainFooterTemplate);
