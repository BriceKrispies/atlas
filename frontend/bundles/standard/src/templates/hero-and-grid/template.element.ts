/**
 * HeroAndGridTemplate — prominent hero band across the top, with a
 * two-up auto-flowing grid of cards below. CSS-only chrome; see
 * ../templates.css for the grid rules.
 */

import { AtlasElement } from '@atlas/core';

export class HeroAndGridTemplate extends AtlasElement {}

AtlasElement.define('template-hero-and-grid', HeroAndGridTemplate);
